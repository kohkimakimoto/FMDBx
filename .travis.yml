language: objective-c
env: LC_CTYPE=en_US.UTF-8
before_install:  
  - gem install cocoapods
  - gem install xcpretty

script:
  - xcodebuild -workspace FMDBx.xcworkspace -scheme FMDBx -sdk iphonesimulator -configuration Debug test

